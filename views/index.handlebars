<!-- This is the jumbotron and main header -->
<div class="container">
    <div class="jumbotron">
    <h1 id="header">
        <span class="glyphicon glyphicon-cutlery"></span> 
        Burger Nominator!
    </h1>
        <hr/>
        <h3>Get Ready To Om Nom Nom.</h3>
    </div>
</div>

<!-- This is where the data is updated to the window -->
<div class="container">
    <div class="col-md-6">
        <ul>
            {{#each burgers}}
                {{#unless this.devoured}}
                <li>
                    {{this.burger_name}}

                    <form action="/{{this.id}}?_method=PUT" method="POST">
                    <input type="hidden" name="devoured" value="true">
                    <button type="submit">Om Nom Nom!</button>
                    </form>
                </li>
                {{/unless}}
            {{/each}}
        </ul>
    </div>
    <div class="col-md-6">
        <ul>
        {{#each burgers}}
            {{#if this.devoured}}
            <li>
                {{this.burger_name}}

                <form action="/{{this.id}}?_method=PUT" method="POST">
                <input type="hidden" name="devoured" value="false">
                <button type="submit">Give Me More!</button>
                </form>
            </li>
            {{/if}}
        {{/each}}
        </ul>
    </div>
</div>

<!-- This is where the client will add their burger -->
<form class="create-update-form" action="/" method="POST">

    <div class="form-group">
        <label for="burg">Burger:</label>
        <input type="text" id="burg" name="name" placeholder="Type in your burger name here...!">
    </div>

    <button type="submit">Submit!</button>

</form>